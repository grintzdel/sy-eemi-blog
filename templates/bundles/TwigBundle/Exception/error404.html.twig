{% extends 'base.html.twig' %}

{% block title %}Page non trouvée - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row min-vh-100 align-items-center">
            <div class="col-lg-6 mx-auto text-center">
                <div class="error-page py-5">
                    <h1 class="display-1 fw-bold text-primary mb-4">
                        <i class="bi bi-exclamation-triangle"></i>
                        404
                    </h1>

                    <h2 class="mb-4">Oups ! Page non trouvée</h2>
                    <p class="lead text-muted mb-5">
                        Désolé, la page que vous recherchez n'existe pas ou a été déplacée.
                    </p>

                    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                        <a href="{{ path('app_home') }}" class="btn btn-primary btn-lg px-4 gap-3">
                            <i class="bi bi-house-door"></i> Retour à l'accueil
                        </a>
                        <a href="{{ path('article_index') }}" class="btn btn-outline-secondary btn-lg px-4">
                            <i class="bi bi-list"></i> Voir les articles
                        </a>
                    </div>

                    <div class="mt-5 pt-5">
                        <p class="text-muted small">
                            <i class="bi bi-info-circle"></i>
                            Si vous pensez qu'il s'agit d'une erreur, veuillez contacter l'administrateur.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% block stylesheets %}
        {{ parent() }}
        <style>
            .error-page {
                animation: fadeIn 0.5s ease-in;
            }

            @keyframes fadeIn {
                from {
                    opacity: 0;
                    transform: translateY(20px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }

            .error-page .display-1 {
                font-size: 8rem;
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
            }

            .error-page i.bi-exclamation-triangle {
                font-size: 6rem;
                display: block;
                margin-bottom: 1rem;
            }
        </style>
    {% endblock %}
{% endblock %}
